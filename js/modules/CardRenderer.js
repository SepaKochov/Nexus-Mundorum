// CardRenderer отключен.
// Вся логика рендера карточек перенесена в main.js (функция renderCards(list)).

export function renderCards(list) {
  console.warn("renderCards() stub from CardRenderer.js, length:", Array.isArray(list) ? list.length : "n/a");
}
